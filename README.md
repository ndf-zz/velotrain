# velotrain

velotrain reads raw transponder passing information from
a set of Chronelec Protime decoders, filters the messages according
to track configuration parameters and then emits valid timing
measurements as JSON-encoded objects to MQTT.


## Requirements

   - metarace (>= 2.0)
   - ypmeteo


## Installation

	$ pip install velotrain


